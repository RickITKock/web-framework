{
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "npx parcel index.html"
  },
  "devDependencies": {
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "json-server": "^0.17.0"
  }
}
